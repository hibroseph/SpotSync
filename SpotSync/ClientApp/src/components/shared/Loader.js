import React from "react";
import ReactLoader from "react-loader-spinner";

const Loader = ({ height, width }) => <ReactLoader type="Audio" color="#E9B84C" height={height} width={width}></ReactLoader>;

export default Loader;
